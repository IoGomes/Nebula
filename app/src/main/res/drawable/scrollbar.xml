<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Grossura da barra (1dp) -->
    <size
        android:width="1dp"
        android:height="20dp"/>

    <!-- Cor da barra -->
    <solid android:color="#ffffff"/>

    <!-- Bordas arredondadas (opcional) -->
    <corners android:radius="0dp"/>
</shape>
